# ota-update-engine-bridge — AGENTS.md

> Base agent rules live in the Helix Constitution submodule
> (`constitution/AGENTS.md` + the `constitution/Constitution.md` it
> references). **READ THOSE FIRST.** Locate the constitution from any
> nested depth via its `find_constitution.sh` helper.
> Canonical reference: https://github.com/HelixDevelopment/HelixConstitution
>
> Restated essentials (the base file is authoritative): no bluffing —
> every PASS carries positive evidence (§11.4); every gate has a paired
> mutation (§1.1); no guessing language (§11.4.6); credentials never
> tracked (§11.4.10); never force-push (§11.4.113); hardlinked backup
> before any destructive op (§9). Project-specific rules extend — never
> weaken — these.
